# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# MET API
GET     /rainfallIDFs/v0.:format                 controllers.FrequenciesController.getRainfallIDFs(sources: Option[String], durations: Option[String], fields: Option[String], format: String)

